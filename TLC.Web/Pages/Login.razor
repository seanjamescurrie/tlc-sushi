@page "/login"

@inject IUserService userService
@inject NavigationManager navManager

@*<div class="login-container">
    <div class="container" style="z-index: 20; position: relative;">
        

        <label class="form-label">EMAIL</label>
        <div class="login-form mb-20">
            <input type="text" class="form-control" placeholder="Enter your email">
        </div>

        <label class="form-label">PASSWORD</label>
        <div class="login-form mb-10">
            <input type="text" class="form-control" placeholder="Enter your password">
        </div>

        <button type="button" class="submit align-self-end btn btn-outline-dark btn-sm rounded-pill mt-2 mr-2">
            <b>SIGN IN</b>
        </button>
        <button type="button" class="submit align-self-end btn btn-outline-dark btn-sm rounded-pill mt-2 mr-2">
            <b>FORGOT PASSWORD</b>
        </button>
    </div>
</div>
*@
<div class="login-card card border-dark mt-4 rounded-sm confirm-order-form mx-auto" style="width:42rem;">
    <div class="card-horizontal">
        <div class="card-body d-flex flex-column">
            <div class="container">
                <div class="order-title">
                    <div class="profile-informations mt-10 mb-15">
                        <h6 class="title"><b>LOGIN</b></h6>
                        <div class="welcome">
                            <p>Welcome sushi-krew</p>
                        </div>
                    </div>
                </div>

                
                <div class="login-form mb-4">
                    <label class="form-label"><b>Email</b></label>
                    <input type="text" class="form-control" placeholder="Enter your email">
                </div>

                
                <div class="login-form mb-4 border-bottom">
                    <label class="form-label"><b>Password</b></label>
                    <input type="text" class="form-control mb-1" placeholder="Enter your password">
                    <a href="">Forgot password?</a>
                </div>

                <div class="">
                    <div class="row justify-content-around">
                        <div class="col-6 form-group">
                            <button type="button" class="basket btn btn-outline-dark btn-sm rounded-pill mt-2 btn-block"
                                    @onclick="@(() => OnValidSubmit())">
                                <b>LOGIN</b>
                            </button>
                        </div>
                    </div>

                    <div class="row justify-content-around">
                        <div class="col-6">
                            <button type="button" class="basket btn btn-warning btn-sm rounded-pill mb-2 btn-block"
                                    @onclick="@(() => OnCancel())">
                                <b>REGISTER</b>
                            </button>
                        </div>
                    </div>
                </div>



            </div>


        </div>
    </div>
</div>


@code {

    public User user = new User();
    protected void OnValidSubmit()
    {
        // order = orderService.CreateOrder(order);
        // Confirmed.InvokeAsync(order);
        navManager.NavigateTo($"/order-management");
    }

    private void OnCancel()
    {
        // basketService.Empty(basket.Id);
        navManager.NavigateTo($"/");
    }
}